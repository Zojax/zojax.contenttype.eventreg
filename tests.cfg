[test]
recipe = zc.recipe.testrunner
eggs = zojax.contenttype.eventreg [test]

[coverage-test]
recipe = zc.recipe.testrunner
eggs = zojax.contenttype.eventreg [test]
defaults = ['--coverage', '../../coverage']

[coverage-report]
recipe = zc.recipe.egg
eggs = z3c.coverage
scripts = coverage=coverage-report
arguments = ('coverage', 'coverage/report')
